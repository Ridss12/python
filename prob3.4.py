#4. Replace the double space from problem 3 with single spaces

name= "Riddhi is a good  girl and  "
print(name.replace("  ", " "))

#strings are immutabke which means that you cannot change them by running functions on them